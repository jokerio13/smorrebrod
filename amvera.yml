meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12
build:
  requirementsPath: smorrebrod/requirements.txt
run:
  scriptName: rune.py
  persistenceMount: /data
  containerPort: 80
